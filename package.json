{
  "name": "quill-and-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "A freelance writer's desk that runs entirely in the browser. The app itself has no dependencies \u2014 Playwright is used only to test it.",
  "scripts": {
    "test": "node tests/run.js",
    "test:engines": "node tests/engines.test.js",
    "test:app": "node tests/app.test.js",
    "test:history": "node tests/history.test.js"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
